apiVersion: v1
kind: Service
metadata:
  name: {{ include "openstreetmaps-tile-server.fullname" . }}
  labels:
    {{- include "openstreetmaps-tile-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openstreetmaps-tile-server.selectorLabels" . | nindent 4 }}
