apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "osm-tiles.fullname" . }}-test-connection"
  labels:
    {{- include "osm-tiles.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "osm-tiles.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
