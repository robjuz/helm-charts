apiVersion: v1
kind: Service
metadata:
  name: {{ include "osm-tiles.fullname" . }}
  labels:
    {{- include "osm-tiles.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "osm-tiles.selectorLabels" . | nindent 4 }}
